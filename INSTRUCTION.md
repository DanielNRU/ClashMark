# 📖 Инструкция по работе с ClashMark

## 1. Быстрый старт

### Linux / MacOS

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Запустите сервер (рекомендуется Gunicorn для production):
   ```bash
   gunicorn -w 1 -b 127.0.0.1:8000 web.routes:app
   ```
   Для разработки можно использовать встроенный сервер Flask:
   ```bash
   python app.py
   # или
   flask run --app web/routes:app
   ```
3. Откройте [http://127.0.0.1:8000](http://127.0.0.1:8000) (или [http://localhost:5001](http://localhost:5001) для Flask) в браузере.

### Windows

1. Установите зависимости:
   ```cmd
   pip install -r requirements.txt
   ```
2. Запустите сервер с помощью Waitress (Gunicorn не поддерживается на Windows):
   ```cmd
   pip install waitress
   python -m waitress --host=127.0.0.1 --port=8000 web.routes:app
   ```
   Для разработки можно использовать встроенный сервер Flask:
   ```cmd
   set FLASK_APP=web/routes.py
   set FLASK_ENV=development
   flask run
   # или
   python app.py
   ```
3. Откройте [http://127.0.0.1:8000](http://127.0.0.1:8000) (или [http://localhost:5001](http://localhost:5001) для Flask) в браузере.

### Docker (Linux/Mac/Windows)

1. Соберите образ:
   ```bash
   docker build -t clashmark .
   ```
2. Запустите контейнер с volume для моделей:
   ```bash
   docker run -p 5001:5001 -v $(pwd)/model:/app/model clashmark
   ```
   Или через docker-compose:
   ```bash
   docker-compose up --build
   ```
3. Откройте [http://localhost:5001](http://localhost:5001) в браузере.

- Все файлы, которые вы кладёте в папку `model/` на вашей машине, будут доступны внутри контейнера по пути `/app/model`.
- Для обновления модели просто замените файл в папке `model/` — контейнер увидит изменения.

---

## 2. Обзор интерфейса

**Современный веб-интерфейс** с унифицированным дизайном включает:

| Страница | Описание |
|----------|----------|
| **Главная** | Анализ коллизий с предварительной статистикой и таблицей пар категорий |
| **Обучение модели** | Загрузка обучающих данных, настройка параметров, запуск обучения |
| **Настройки** | Выбор модели, порогов уверенности, просмотр метрик и матрицы ошибок |

### **Ключевые элементы интерфейса:**
- **Предварительный анализ** — статистика по файлам после загрузки
- **Таблицы пар категорий** — с поиском и сортировкой
- **Матрица ошибок** — цветовая схема TP/TN (зелёный) и FP/FN (розовый)
- **Live-графики** — прогресс обучения в реальном времени
- **Адаптивный дизайн** — работает на всех устройствах

---

## 🖼️ Примеры интерфейса

| Описание | Скриншот |
|----------|----------|
| Главная страница: загрузка XML и ZIP, просмотр статистики, запуск анализа | ![Главная страница](images/screenshot_main.png) |
| Ручная разметка: окно для корректировки спорных коллизий | ![Ручная разметка](images/screenshot_manual_review.png) |
| Страница обучения: загрузка данных, настройка параметров, запуск обучения | ![Обучение модели](images/screenshot_train.png) |
| Live-графики: прогресс обучения, метрики, матрица ошибок | ![Live-графики](images/screenshot_live_metrics.png) |
| Страница настроек: выбор модели, пороги, просмотр метрик | ![Настройки](images/screenshot_settings.png) |


## 3. Главная страница — Анализ коллизий

### **3.1 Загрузка данных**
1. Перейдите на главную страницу
2. Загрузите XML-файл(ы) с коллизиями и ZIP-архив(ы) с изображениями
3. **Автоматически отобразится предварительная статистика:**
   - Количество XML файлов и ZIP архивов
   - Общее число коллизий
   - Найдено изображений
   - Таблица пар категорий с количеством коллизий
   - Статистика по каждому XML-файлу

![Главная страница](images/screenshot_main.png)

### **3.2 Настройка параметров**
- **Порог низкой уверенности** (по умолчанию 0.3) — ниже этого значения коллизия считается разрешённой
- **Порог высокой уверенности** (по умолчанию 0.7) — выше этого значения коллизия считается активной
- **Режим инференса:**
  - *Модель* — использование обученной нейросети
  - *Алгоритм* — использование правил на основе пар категорий
- **Ручная разметка** — включить для случаев с неуверенными предсказаниями

### **3.3 Выполнение анализа**
1. Нажмите "Анализировать коллизии"
2. Дождитесь завершения обработки
3. При включённой ручной разметке откорректируйте спорные случаи
4. Скачайте результат в XML формате

---

## 4. Обучение модели

### **4.1 Подготовка данных**
1. Перейдите на вкладку "Обучение модели"
2. Загрузите XML-файлы и ZIP-архивы с изображениями
3. **Просмотрите предварительную статистику:**
   - Общее количество коллизий
   - Количество готовых для обучения
   - Распределение по классам
   - Таблица пар категорий

![Страница обучения](images/screenshot_train.png)

### **4.2 Настройка обучения**
- **Batch size** — размер батча (рекомендуется 16-32)
- **Epochs** — количество эпох обучения (рекомендуется 5-20)

### **4.3 Процесс обучения**
1. Нажмите "Начать обучение"
2. **Автоматически откроется страница live-прогресса:**
   - Графики loss (train/validation) в реальном времени
   - График accuracy по эпохам
   - Ключевые метрики: accuracy, F1-score, recall, precision
   - **Матрица ошибок** с цветовой схемой
   - Статус обучения

### **4.4 Результаты обучения**
- Модель сохраняется в папку `model/` с временной меткой
- Статистика обучения записывается в `model_train_log.json`
- Доступна для выбора в настройках

---

## 5. Настройки

### **5.1 Выбор модели**
- Выпадающий список всех доступных моделей
- Для каждой модели отображаются параметры обучения и метрики
- Автоматическое обновление статистики при выборе

### **5.2 Просмотр метрик**
- **Accuracy, F1-score, Recall, Precision** — отображаются в одну строку
- **Таблица пар категорий** — с поиском и сортировкой
- **Матрица ошибок** — красивая визуализация с подписями классов

### **5.3 Настройка порогов**
- **Порог низкой уверенности** — для определения разрешённых коллизий
- **Порог высокой уверенности** — для определения активных коллизий
- Между порогами — коллизии требуют ручной проверки

### **5.4 Дополнительные настройки**
- **Режим инференса** — модель или алгоритм
- **Формат экспорта** — стандартный или BIMStep
- **Ручная разметка** — включить/выключить

---

## 6. Матрица ошибок

### **6.1 Цветовая схема**
- **Зелёный** (TP/TN) — правильные предсказания (диагональ матрицы)
- **Розовый** (FP/FN) — ошибки классификации (не диагональ)

### **6.2 Подписи классов**
- **2 класса:** Approve, Active
- **3 класса:** Approve, Active, Reviewed
- **N классов:** Класс 0, Класс 1, ...

### **6.3 Интерпретация**
- **TP (True Positive)** — правильно классифицированные активные коллизии
- **TN (True Negative)** — правильно классифицированные разрешённые коллизии
- **FP (False Positive)** — разрешённые коллизии, классифицированные как активные
- **FN (False Negative)** — активные коллизии, классифицированные как разрешённые

---

## 7. Требования к данным

### **7.1 XML файлы**
- Формат Navisworks или BIMStep
- Должны содержать visual-пары с изображениями
- Примеры в папке `Обучение/`

### **7.2 Изображения**
- Соответствие путям из XML (обычно папка BSImages)
- Поддерживаемые форматы: PNG, JPG
- Рекомендуемый размер: 500x500 или 1024x1024 px

### **7.3 Баланс классов**
- Для успешного обучения нужны примеры обоих классов (0 и 1)
- Класс -1 (требует анализа) исключается из обучения
- Рекомендуется примерно равное количество примеров

---

## 8. Экспорт и форматы

### **8.1 Результаты инференса**
- XML файл с дополнительными полями:
  - `cv_prediction` — предсказание модели (0/1)
  - `cv_confidence` — уверенность модели (0-1)
- Сохранение структуры исходного файла

### **8.2 Форматы экспорта**
- **Стандартный** — базовый формат с метриками
- **BIMStep** — сохранение полной структуры BIMStep

### **8.3 Статистика**
- Сводная информация по файлам
- Распределение по классам
- Таблица пар категорий

---

## 9. Структура проекта

```
ClashMark/
├── app.py                       # Flask-сервер
├── core/                        # Утилиты
│   ├── image_utils.py           # Обработка изображений
│   └── xml_utils.py             # XML парсинг/экспорт
├── ml/                          # ML модули
│   ├── model.py                 # MobileNetV3 архитектура
│   ├── train.py                 # Обучение
│   ├── inference.py             # Инференс
│   └── dataset.py               # Датасет
├── web/                         # Веб-интерфейс
│   ├── settings.py              # Настройки
│   └── utils.py                 # Утилиты
├── templates/                   # HTML шаблоны
├── static/                      # CSS, JS, изображения
├── model/                       # Обученные модели
└── category_pairs.yaml          # Категории
```

---

## 10. Частые вопросы

**Q:** Почему после фильтрации не осталось данных для обучения?
**A:** Среди visual-пар нет примеров обоих классов (0 и 1). Добавьте такие примеры в XML.

**Q:** Как интерпретировать матрицу ошибок?
**A:** Зелёные ячейки (диагональ) — правильные предсказания, розовые — ошибки.

**Q:** Где посмотреть метрики обучения?
**A:** На странице live-прогресса обучения (открывается автоматически).

**Q:** Как изменить пороги уверенности?
**A:** В настройках на главной странице или в разделе "Настройки".

**Q:** Почему матрица ошибок показывает "Нет данных"?
**A:** Модель не содержит данных confusion_matrix. Проверьте файл статистики модели.

---

## 11. Советы по использованию

### **11.1 Подготовка датасета**
- Используйте только реально встречающиеся пары категорий
- Проверяйте соответствие имён файлов в XML и изображениях
- Добавьте примеры обоих классов для балансировки

### **11.2 Обучение модели**
- Начните с небольшого количества эпох (5-10)
- Следите за графиками loss/accuracy
- При переобучении уменьшите количество эпох

### **11.3 Анализ результатов**
- Обращайте внимание на матрицу ошибок
- Анализируйте распределение по классам
- Используйте ручную разметку для сложных случаев

---

## 12. Контакты

По вопросам и поддержке: [git@danieln.ru](mailto:git@danieln.ru) 